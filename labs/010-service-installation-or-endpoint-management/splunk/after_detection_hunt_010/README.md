# Splunk Lab App

Run `generate_lab.py` from the lab directory to populate the app's `lookups/` directory, then copy `after_detection_hunt_010` into `$SPLUNK_HOME/etc/apps/` and restart Splunk. The app uses bundled CSV lookups and requires no indexed data.
