# Data Dictionary

Eight platform-portable CSV lookups are generated deterministically, each with exactly 10,000 records: detections, authentication, process, file, network, identity administration, response activity, and investigation findings. All event timestamps are UTC ISO 8601.